services:
  metube:
    image: ghcr.io/alexta69/metube
    container_name: metube
    restart: unless-stopped
    ports:
      - "8081:8081"
    environment:
      STATE_DIR: /metube-state
    volumes:
      - /shared/metube:/downloads
      - /data/metube/state:/metube-state

